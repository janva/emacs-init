(defun fun (args)
  (defun fun (args)  
  (defun fun (args)
    "docstring"
    (interactive "P")
    
  )

(defun fun (args)
  "docstring"
  (interactive "P")
  )

(line)
(s)


(setq company-icon-margin 2)
(setq company-format-margin-function #'company-vscode-dark-icons-margin)
(setq company-format-margin-function #'company-text-icons-margin)
(setq company-show-quick-access nil)
(def)
(def)


dsafds
dskjfakj something
